{
  "name": "hexo-site",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "lint": "prettier --write '**/*.{js,jsx,less,md}'",
    "build": "hexo generate",
    "clean": "hexo clean",
    "deploy": "hexo deploy",
    "cm": "yarn lint && git add . && git commit -m '👆 cmd up~'",
    "cmp": "yarn cm && git push",
    "dev": "hexo server --port 9123 --draft"
  },
  "hexo": {
    "version": "6.0.0"
  },
  "dependencies": {
    "hexo": "^6.0.0",
    "hexo-generator-archive": "^1.0.0",
    "hexo-generator-category": "^1.0.0",
    "hexo-generator-index": "^2.0.0",
    "hexo-generator-tag": "^1.0.0",
    "hexo-renderer-ejs": "^2.0.0",
    "hexo-renderer-marked": "^4.0.0",
    "hexo-renderer-stylus": "^2.0.0",
    "hexo-server": "^2.0.0",
    "hexo-theme-landscape": "^0.0.3"
  },
  "devDependencies": {
    "prettier": "^2.8.4"
  }
}
